<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
    <title>{% block title %}Tchat!{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset('bundles/framework/css/semantic.css') }}" />
    {% block stylesheets %}
        <style type="text/css">
            body {
                background-color: #FFFFFF;
            }
            .ui.menu .item img.logo {
                margin-right: 1.5em;
            }
            .main.container {
                margin-top: 7em;
            }
            .wireframe {
                margin-top: 2em;
            }
        </style>
    {% endblock %}
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />

</head>
<body>
<div class="ui fixed inverted menu">
    <div class="ui container">
        <a href="#" class="header item">
            Slack I@D
        </a>
        {% if app.user %}<a href="{{ path('fos_user_security_logout') }}" class="item">Déconnexion</a>{% else %} <a href="{{ path('fos_user_security_login') }}" class="item">Connexion</a> <a href="{{ path('fos_user_registration_register') }}" class="item">Inscription</a> {% endif %}
    </div>
</div>
<div class="ui fluid main container">
    <div class="ui four column grid">
        <div class="row">
            <div class="column">
                {% if app.user %}
                    <div class="ui vertical menu">
                        {{ render(controller('TchatBundle:User:whoIsOnline')) }}
                        {{ render(controller('TchatBundle:Room:index')) }}
                    </div>
                {% endif %}
            </div>
            <div class="column">{% block body %}{% endblock %}</div>
        </div>
    </div>
</div>
{% block javascripts %}{% endblock %}
<script src="{{ asset('bundles/framework/js/jquery.min.js') }}"></script>
<script src="{{ asset('bundles/framework/js/semantic.min.js') }}"></script>
</body>
</html>